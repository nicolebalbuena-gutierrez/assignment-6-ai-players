# Suppress native access warnings from Gradle's native-platform library
# These warnings are from Gradle itself, not the project code
org.gradle.jvmargs=-Xmx2g -XX:+EnableDynamicAgentLoading --enable-native-access=ALL-UNNAMED

# Enable configuration cache for faster builds
org.gradle.configuration-cache=true
